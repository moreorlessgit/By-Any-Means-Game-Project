When units arrive on scene or the call status changes, have a button appear near the top right of the call window that refreshes the window to display the changes.

Unit AVLs (labels on map) should have a configurable background to them. Default should be Black outline with grey background and text on top. I eventually would like the background to change based upon the unit status (Enroute, On Scene, Returning.) This may lead to a new button on the header for settings, which allows for players to customize colors for a lot of things, among other things yet to be determined.

Player based call generation times. What i mean by this is the player can adjust the spawn rate of calls that just adjusts the default timings defined in the config file. Anything from 0.20x (simulating a slow day) to 3x (pure chaos)

EXTREME BUG - Units not moving after being dispatched to calls. Must be a specific glitch for one unit, as a different LEO unit was able to move.

Fix - Units not being able to be reassigned from a call once they are dispatched and enroute. Units should be able to be reassigned/canceled as needed. Additionally to this, a unit returning from a call was not able to be sent to a new call because it was considered in use even though it was just returning to the station.

Fix - Nothing but law enforcement call types spawning in a single ESN when i have 3 total ESNs and fire units.

Fix - Call requirements considered covered even with additional units not on scene, which effects the speed that a call gets completed. An engine was on scene of a call that requires an engine and a rescue. Added a mutual aid rescue engine that is 9 minutes away and it considered it as a rescue being on scene and staffing at 100%.

Fix - Units traveling animations jump between high speeds and acceleration to slow speeds visually. Looks very weird.